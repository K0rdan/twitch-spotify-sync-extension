import React from 'react';
import { NextPage } from 'next';

type PanelProps = {};
export const PanelPage: NextPage<PanelProps> = ({}: PanelProps) => (
  <div>TEST</div>
);

export default PanelPage;
